<div class="text-center">
  <h1><%= @user %></h1>

  <p class="text-secondary text-small">Account created: <%= @user.created_at.strftime("%B %d, %Y") %></p>
</div>

<%= form_with model: @user, scope: :user do |f| %>
  <%= render "form", f: f %>
<% end %>
