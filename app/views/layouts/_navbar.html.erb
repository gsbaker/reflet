<nav class="navbar d-flex justify-content-between p-3 mb-3">
  <div class="container">
    <%= link_to "/", class: "navbar-brand" do %>
      <%= image_tag "logo-lg.png", height: 30 %>
    <% end %>
    <% if signed_in? %>
      <div class="d-flex align-items-center">
        <div class="me-4">
          <a href="/" class="r-nav-link <%= 'r-nav-link-active' if current_page?(root_path) %>">Dashboard</a>
        </div>
        <div class="me-4">
          <a href="#" class="r-nav-link">My Needs</a>
        </div>
        <div class="me-4">
          <a href="#" class="r-nav-link">Thought Records</a>
        </div>
        <div class="me-2">
          <%= link_to current_user.name, user_path(current_user)  %> 
        </div>
        <div>
          <%= button_to "Sign out", sign_out_path, method: :delete, class: "r-btn btn-link text-decoration-none" %>
        </div>
      </div>
    <% else %>
      <div>
        <%= link_to "Sign in", sign_in_path %>
      </div>
    <% end %> 
  </div>
</nav>
